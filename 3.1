"""
write a function called liner _search _product that takes the list of product
and a target product name as input.The Function Should perform a liner the target product in the list and
return a list of indices of all occurrences of the product if found, or an empty list if product is not
found. 
"""


def linearSearchProduct(productlist,targetProduct):
  indices = []

for index,product in enumerate(productlist):
  if Product == targetProduct:
      indices.append(index)

 return indices


#Example usage:
Products =["shoes","boot","loafer","shoes","sandal","shoes"]
target = "shoes"
target = 'apple'
result = linearSearchProduct(products,target2)
print(result)
